<%= simple_form_for(@gender) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: false, input_html: {class: :form_input}, placeholder: "Name" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
